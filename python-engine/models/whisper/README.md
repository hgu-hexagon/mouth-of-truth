# Whisper 캐시

선택 디렉터리:

```text
models--openai--whisper-tiny/
```

모델:

```text
openai/whisper-tiny
```

전사 활성화:

```bash
export MOUTH_OF_TRUTH_ENABLE_TRANSCRIPTION=1
```

Hugging Face 캐시 구조를 그대로 유지합니다.
